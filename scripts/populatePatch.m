
animal = 'animal_id=5113';

populate(patch.Recording, animal);
populate(patch.Quality,animal);

populate(patch.Spikes,animal);

populate(patch.PeriLed,animal);

populate(patch.Threshold, animal);
populate(patch.PeriLedTrialSet,animal);